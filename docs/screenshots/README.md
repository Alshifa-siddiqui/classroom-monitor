# Screenshots

Place the dashboard screenshots here, named exactly so the main README picks them up:

| File | Shows |
|---|---|
| `01-login.png` | Sign-in screen |
| `02-signup.png` | Account creation (optional admin code) |
| `03-dashboard.png` | Admin dashboard — session controls, live stat cards, charts |
| `04-students.png` | Registered students with auto-generated IDs |
| `05-enrollment.png` | Face enrollment success (samples captured + quality) |

The live recognition overlay (name · ID · emotion · attention · confidence on the
video feed) is best shown in the demo video, since it needs a person in frame.
A faithful render of that overlay is at [../overlay_sample.png](../overlay_sample.png).
